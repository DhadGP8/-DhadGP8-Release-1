using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;

public class TestScript : MonoBehaviour
{
   [SerializeField] private  ConfirmationWindow1 myconfwindo;
    // Start is called before the first frame update
    void Start()
    {
       // OpenConfirmationWindow(" ﻑﺮﺤﻟﺍ ﺽﺮﻋ ﻢﺘﻴﻟ ﺔﻗﺎﻄﺒﻟﺍ ﻰﻟﺇ ﺍﺮﻴﻣﺎﻜﻟﺍ ﻪﺟﻭ");
    }
  //  private void OpenConfirmationWindow(string massag)
    //{
      //  myconfwindo.gameObject.SetActive(true);
        //myconfwindo.OkButton.onClick.AddListener(OkClicked);
        //myconfwindo.massageText.text = massag;

    //}
    //private void OkClicked()
    //{

      //  myconfwindo.gameObject.SetActive(false);

        //Debug.Log("OkCliked");
       //SceneManager.LoadScene("ScanGame");


    //}

}
